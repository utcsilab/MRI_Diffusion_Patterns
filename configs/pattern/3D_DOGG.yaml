sample_pattern: 'Learned3d'

num_acs_lines: 16

R: 20

#length: this is set from data
#device: this is taken from setup

cut_corners: true

init_dist: 'uniform' #all logits taker same value at start

sampler: 'gumbel'

tau: 1.0
